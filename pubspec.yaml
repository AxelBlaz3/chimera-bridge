name: bridge_generator
description: Generates Chimera Bridge code for RN (Turbo) -> Native -> Flutter.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  mason: ^0.1.0

dev_dependencies:
  analyzer: ^10.0.2
  build_runner: ^2.10.5
  glob: ^2.1.3
  lints: ^6.0.0
  mason_cli: ^0.1.0
  mockito: ^5.6.3
  test: ^1.29.0
